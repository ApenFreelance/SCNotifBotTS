
import { BotEvent, CustomEvents, EventType } from '../types'

const event: BotEvent = {
    name: CustomEvents.RemoveVerification,
    type: EventType.ON,
    async execute(_) {    
        
    
    }
}
export default event
